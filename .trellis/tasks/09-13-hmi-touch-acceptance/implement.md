# 触摸与最终验收实施步骤

## 进入条件

- [x] 取得本子任务实施授权，检查前两项的实际验证结果和接口。
- [x] 按当前 Git 状态与 Trellis 启动本子任务，不并行修改共享 UI 文件。
- [x] 重新确认 RGB565、16 ms 刷新、10 ms tick、1000 ms 业务 tick 和有效点表容量。

## T1：调度与输入

- [x] 添加输入 timer 10 ms 设置及 1..10 ms 主循环 sleep 限制。
- [x] 覆盖无 indev、零 idle、最大 idle 和持续触摸状态，禁止忙等待。（主机 `test_hmi_sched`；板端无 indev 打日志）
- [x] 记录真实输入回调间隔与调度取整，和原基线比较。（板端 `pointer indev read=10ms n=1`；L1 交互间隔仍待实体触摸）
- [x] 确认未调整系统 tick、外设配置、采集频率或线程优先级。

## T2：导航状态

- [x] 在已有根生命周期上增加有界导航记录和页面状态保存/恢复。
- [x] 验证首页筛选/位置、详情点位、趋势窗口和报告版本位置。（无头生命周期仍过；专项交互用例待补）
- [x] 覆盖同页点击、栈满/空栈、点位删除和告警恢复。（代码路径已加；自动化用例待扩）
- [x] 确认扫描开关、确认对话框和候选提交意图没有被恢复或自动重发。
- [x] 切页后阻止旧触点释放穿透新页面。（`lv_indev_wait_release`）

## T3：滚动、防误触和反馈

- [x] 使用 LVGL 原生滚动事件，拖动或 press lost 取消点击。（沿用原生 CLICKED；SCROLL_BEGIN 取消按下绑定）
- [x] 按下记录 ID、表/页面代次、告警期，释放重新校验。
- [x] 手势期间暂缓列表结构变化，结束仅应用最新版本并恢复锚点。
- [x] 惯性点停不触发业务按钮；新告警仍及时进入模型和状态栏。（SCROLL_BEGIN 清 press；冻结期按 ID 原地刷新）
- [x] 检查内容热区和 28 px 顶部例外；验证最长文本、不重叠、不吞点击。（返回钮 48x28；告警 chip 44x28）
- [x] 报告加载/生成使用即时忙状态和真正 disabled，返回始终可操作。（前置 runtime 已交付）

## T4：自动回归

- [x] 完成 research/interaction-cases.md 的可无头子集（I01/I02/I11/I12/I17/I18/I19）；实体触摸项仍归 T5。
- [x] 复跑前两项的无头、host_tests，防止交互层破坏底层修复。
- [x] 200 次自动导航压力后，同页资源回到预热值，无持续增长。（lifecycle_render_check 仍覆盖）
- [x] 检查 RGB565 和 32 位测试图像，至少首页、告警、趋势、报告的空/忙/错误状态。（16/32 位 ctest 5/5，含 interaction_check；像素级人工目视仍可选）

前置入口已存在时使用：

```bash
cmake --build .debug/hmi-headless
ctest --test-dir .debug/hmi-headless --output-on-failure
cmake --build .debug/hmi-headless-32
ctest --test-dir .debug/hmi-headless-32 --output-on-failure
make -C app/velaguard/host_tests test
bash scripts/build.sh --hmi-perf
```

## T5：真机最终验收

- [x] 按板端内环烧录本次成功构建产物，不使用旧镜像。（测量固件采 L0 后已复位默认产品 hex `3cd82ff4…`；`stage1_lvgl_hmi_accept` pass=16）
- [ ] 在 D1/D2、1/3/8 行告警及 D5 报告状态下完成 L0-L4。（仅 D1 L0 静止对照完成；交互与 D2/D3 未测）
- [ ] 按统一契约收集至少 200 次实体点击、200 次切页和指定滚动录像。
- [ ] 完成 30 分钟混合操作，每 5 分钟记录同页静止资源。
- [ ] 核对 ge/le/eq、离线和恢复、主告警文件，以及点表人工确认边界。
- [ ] 同固件 L3 必须保留真实 Agent；失败与既有挂死任务关联，不移除该组。
- [ ] 统计每组 P95/FPS/长帧及资源变化，给出与基线的同条件对照。
- [ ] 默认统计关闭构建复核业务和体验，记录最终交付固件身份。

## 结果与交接

- [ ] 实际运行后创建 `research/final-results.md`，逐项映射 T-AC1 至 T-AC8 和父任务 HMI-AC1 至 HMI-AC9。
- [ ] 保存原始证据路径、工具/相机参数、测量误差及未测/失败项。
- [ ] 给父任务明确完整通过或未完成的结论，不能只写编译通过。
- [ ] 执行 Trellis 全范围质量检查；需提交/归档时单独遵循用户授权，不自动推送。

如果性能或安全标准未满足，输出当前改进、剩余瓶颈和下一步建议，保持任务未完成；不得降低验收阈值或补写虚构证据。
